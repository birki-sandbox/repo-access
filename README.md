# repo-access

A demo using the GitHub terraform provider to manage access to repos within an org

## Usage

Valid Permissions:

- `pull` (read) default
- `triage`
- `push` (write)
- `maintain`
- `admin`
